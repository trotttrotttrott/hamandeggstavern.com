%h1
  %span events
  = link_to 'New', new_admin_event_path, :class => 'btn btn-primary btn-mini'

= paginate @events

%table.table
  %tr
    %th Name
    %th From
    %th To
    %th Words
    %th Age Restriction
    %th Acts
    %th
    %th
    %th

  - @events.each do |event|
    %tr
      %td= event.name
      %td= event.start.strftime('%c')
      %td= event.finish.strftime('%c')
      %td= event.words
      %td= event.age_restriction
      %td
        - if event.participations.any?
          %ul.unstyled
            - event.participations.by_rank.each do |event_participation|
              %li= event_participation.act.name
      %td= link_to 'Show', admin_event_path(event)
      %td= link_to 'Edit', edit_admin_event_path(event)
      %td= link_to 'Destroy', admin_event_path(event), :method => :delete, :data => { :confirm => 'Are you sure?' }

= paginate @events
