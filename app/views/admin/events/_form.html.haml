- if @event.errors.any?
  #error_explanation
    %h2= "#{pluralize(@event.errors.count, "error")} prohibited this event from being saved:"
    %ul
      - @event.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.label :name
  = f.text_field :name, :autofocus => true
.field
  = f.label :date
  = f.date_select :date, :default => Chronic.parse('today')
.field
  = f.label :words
  = f.text_area :words, :class => 'span12'
.actions
  = f.submit 'Save'
