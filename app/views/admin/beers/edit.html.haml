%h1 Editing beer

= form_for @beer, :url => admin_beer_path(@beer) do |f|
  = render :partial => 'form', :locals => { :f => f }

= link_to 'Show', admin_beer_path(@beer)
\|
= link_to 'Back', admin_beers_path
