- if @admin_patron.errors.any?
  #error_explanation
    %h2= "#{pluralize(@admin_patron.errors.count, "error")} prohibited this admin_patron from being saved:"
    %ul
      - @admin_patron.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.label :email
  = f.text_field :email
.actions
  = f.submit 'Save'
